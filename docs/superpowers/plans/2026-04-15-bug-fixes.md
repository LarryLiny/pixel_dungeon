# Bug Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all known bugs in the dungeon-roguelike game, prioritized from critical to low.

**Architecture:** Each task is self-contained — fix a specific bug in one or two files. No task depends on another except Task 1 (fusion item defs) which must land before Task 4 (HUD fusion display) and Task 5 (HUD dynamic slots) since those tasks display fusion items.

**Tech Stack:** TypeScript, Phaser 3, Vite

---

## Task 1: Add Fusion Item Definitions (CRITICAL)

**Files:**
- Modify: `src/data/items.ts` — Add `FUSION_ITEMS` record with all 20 fusion outputs
- Modify: `src/data/fusionRecipes.ts` — Import `FUSION_ITEMS` and add helper

Fusion outputs have no `ItemDef` in `ALL_ITEMS`, making all fusion weapons completely non-functional. We need to add definitions for each fusion item.

- [ ] **Step 1: Add fusion item definitions to `src/data/items.ts`**

After the `ALL_ITEMS` declaration (line 394), add the fusion items record and merge it:

```typescript
/* ============================================================
   融合产物（20种）— 两个道具融合后的产出
   ============================================================ */
export const FUSION_ITEMS: Record<string, ItemDef> = {
  // ── Common × Common → Uncommon ──
  freeze_shotgun: {
    id: 'freeze_shotgun', name: '急冻霰弹', category: 'weapon', rarity: 'uncommon',
    description: '子弹减速敌人60%', color: '#88ccff', icon: '冻',
    maxLevel: 9, levels: [3, 5, 7, 9, 11, 14, 17, 20, 24], locked: false,
  },
  hellfire: {
    id: 'hellfire', name: '地狱火', category: 'weapon', rarity: 'uncommon',
    description: '火球点燃地面持续灼烧', color: '#ff6622', icon: '焰',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 11, 14], locked: false,
  },
  death_wheel: {
    id: 'death_wheel', name: '死亡轮盘', category: 'weapon', rarity: 'uncommon',
    description: '巨大回旋镖斩杀低血量目标', color: '#22ddaa', icon: '轮',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 11, 14], locked: false,
  },
  wind_runner: {
    id: 'wind_runner', name: '风行者', category: 'defense', rarity: 'uncommon',
    description: '移动时回血速度翻倍', color: '#66ffaa', icon: '风',
    maxLevel: 9, levels: [1.5, 2.0, 2.5, 3.0, 3.8, 4.5, 5.5, 7.0, 9.0], locked: false,
  },
  mega_blaster: {
    id: 'mega_blaster', name: '巨炮', category: 'weapon', rarity: 'uncommon',
    description: '单发巨型弹，高伤害+穿透', color: '#ffcc44', icon: '炮',
    maxLevel: 9, levels: [1.0, 1.3, 1.6, 2.0, 2.5, 3.0, 3.8, 5.0, 7.0], locked: false,
  },
  shield_bash: {
    id: 'shield_bash', name: '铁壁', category: 'defense', rarity: 'uncommon',
    description: '护盾反弹伤害给敌人', color: '#6688cc', icon: '铁',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 11, 14], locked: false,
  },

  // ── Uncommon × Uncommon → Rare ──
  thunder_slash: {
    id: 'thunder_slash', name: '雷暴斩', category: 'weapon', rarity: 'rare',
    description: '剑气命中触发连锁闪电', color: '#88aaff', icon: '雷',
    maxLevel: 9, levels: [1.5, 2.0, 2.8, 3.5, 4.5, 5.5, 7.0, 9.0, 12.0], locked: false,
  },
  fragment_bomb: {
    id: 'fragment_bomb', name: '碎裂炮', category: 'weapon', rarity: 'rare',
    description: '命中分裂成小弹片，范围伤害', color: '#ff7744', icon: '碎',
    maxLevel: 9, levels: [3, 5, 7, 9, 11, 14, 17, 20, 24], locked: false,
  },
  thunderstorm: {
    id: 'thunderstorm', name: '雷暴', category: 'weapon', rarity: 'rare',
    description: '穿透型闪电攻击', color: '#6699ff', icon: '暴',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 12, 15], locked: false,
  },
  tracking_fireball: {
    id: 'tracking_fireball', name: '追踪火球', category: 'weapon', rarity: 'rare',
    description: '火球自动追踪最近敌人', color: '#ff5533', icon: '追',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 11, 14], locked: false,
  },
  void_walker: {
    id: 'void_walker', name: '虚空行者', category: 'defense', rarity: 'rare',
    description: '无敌频率大幅提升', color: '#99aacc', icon: '虚',
    maxLevel: 9, levels: [1.0, 1.2, 1.4, 1.6, 1.8, 2.0, 2.3, 2.6, 3.0], locked: false,
  },

  // ── Rare × Rare → Epic ──
  frost_storm: {
    id: 'frost_storm', name: '雷暴冰霜', category: 'weapon', rarity: 'epic',
    description: '冰冻后闪电爆裂双重伤害', color: '#44ccff', icon: '霜',
    maxLevel: 9, levels: [1.2, 1.6, 2.0, 2.5, 3.2, 4.0, 5.0, 6.5, 8.5], locked: false,
  },
  plague_bomb: {
    id: 'plague_bomb', name: '瘟疫炸弹', category: 'weapon', rarity: 'epic',
    description: '毒雾范围爆炸持续伤害', color: '#66aa22', icon: '瘟',
    maxLevel: 9, levels: [1.0, 1.5, 2.0, 2.5, 3.2, 4.0, 5.0, 6.5, 8.0], locked: false,
  },
  black_hole: {
    id: 'black_hole', name: '黑洞', category: 'defense', rarity: 'epic',
    description: '吸引+减速+伤害+爆炸', color: '#8844cc', icon: '洞',
    maxLevel: 9, levels: [70, 100, 130, 170, 210, 260, 320, 400, 500], locked: false,
  },
  sun_storm: {
    id: 'sun_storm', name: '太阳风暴', category: 'weapon', rarity: 'epic',
    description: '超大火球灼烧范围内所有敌人', color: '#ff8822', icon: '阳',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 11, 14], locked: false,
  },
  nuclear_core: {
    id: 'nuclear_core', name: '核弹核心', category: 'augment', rarity: 'epic',
    description: '命中触发范围爆炸', color: '#ff3355', icon: '核',
    maxLevel: 9, levels: [25, 40, 55, 75, 95, 120, 150, 185, 230], locked: false,
  },

  // ── Epic × Epic → Legendary ──
  photon_cannon: {
    id: 'photon_cannon', name: '光子炮', category: 'weapon', rarity: 'legendary',
    description: '持续扫射激光束', color: '#ff44ff', icon: '光',
    maxLevel: 9, levels: [1.5, 2.0, 2.8, 3.5, 4.5, 5.5, 7.0, 9.0, 12.0], locked: false,
  },
  absolute_defense: {
    id: 'absolute_defense', name: '绝对防御', category: 'defense', rarity: 'legendary',
    description: '护盾反弹+无敌帧大幅增加', color: '#ccddff', icon: '绝',
    maxLevel: 9, levels: [1, 2, 3, 4, 5, 7, 9, 11, 14], locked: false,
  },

  // ── Legendary × Legendary ──
  soul_reaper: {
    id: 'soul_reaper', name: '灵魂收割', category: 'weapon', rarity: 'legendary',
    description: '斩杀触发范围爆炸+回复', color: '#aa22ff', icon: '魂',
    maxLevel: 9, levels: [0.15, 0.20, 0.25, 0.30, 0.35, 0.40, 0.45, 0.50, 0.60], locked: false,
  },
  angel_embrace: {
    id: 'angel_embrace', name: '天使之拥', category: 'defense', rarity: 'legendary',
    description: '低于50%血自动大回血+无敌3s', color: '#ffffaa', icon: '使',
    maxLevel: 9, levels: [0.20, 0.25, 0.30, 0.35, 0.40, 0.45, 0.50, 0.55, 0.65], locked: false,
  },
};

/** All items including fusion outputs */
export const ALL_ITEMS: Record<string, ItemDef> = {
  ...WEAPONS,
  ...AUGMENTS,
  ...DEFENSES,
  ...FUSION_ITEMS,
};
```

NOTE: This replaces the existing `ALL_ITEMS` declaration on line 390-394. The old code was:

```typescript
/** All items merged */
export const ALL_ITEMS: Record<string, ItemDef> = {
  ...WEAPONS,
  ...AUGMENTS,
  ...DEFENSES,
};
```

- [ ] **Step 2: Update ItemSystem to handle fusion items in recalculate()**

In `src/systems/ItemSystem.ts`, the `switch` statement already falls through to the `isFusionOutput` check. Now that fusion items ARE in `ALL_ITEMS`, we need to add cases for fusion items that have unique combat effects. Modify `recalculate()` to add weapon type overrides for fusion items that change weapon behavior:

After the existing `case 'death_scythe':` block (line 82-85), add fusion weapon cases:

```typescript
        // ── Fusion Weapons ──
        case 'freeze_shotgun':
          mod.shotCount += value - 1;
          mod.slowOnHit = Math.max(mod.slowOnHit, 0.6);
          mod.slowDuration = Math.max(mod.slowDuration, 2.0);
          mod.weaponType = 'freeze_shotgun';
          break;
        case 'hellfire':
          mod.orbCount += value;
          mod.weaponType = 'hellfire';
          break;
        case 'death_wheel':
          mod.boomerangCount += value;
          mod.executeThreshold = Math.max(mod.executeThreshold, 0.15);
          mod.weaponType = 'death_wheel';
          break;
        case 'mega_blaster':
          mod.damageMul *= value;
          mod.pierce += 3;
          mod.weaponType = 'mega_blaster';
          break;
        case 'thunder_slash':
          mod.swordArcAngle = 90;
          mod.swordRange = 60 + value * 10;
          mod.damageMul *= value;
          mod.weaponType = 'thunder_slash';
          break;
        case 'fragment_bomb':
          mod.shotCount += value - 1;
          mod.splashRadius = Math.max(mod.splashRadius, 40);
          mod.weaponType = 'fragment_bomb';
          break;
        case 'thunderstorm':
          mod.chainCount += value;
          mod.weaponType = 'thunderstorm';
          break;
        case 'tracking_fireball':
          mod.orbCount += value;
          mod.weaponType = 'tracking_fireball';
          break;
        case 'frost_storm':
          mod.iceSlowAmount = Math.max(mod.iceSlowAmount, 0.3 + value * 0.05);
          mod.chainCount += Math.floor(value * 0.5);
          mod.damageMul *= value;
          mod.weaponType = 'frost_storm';
          break;
        case 'plague_bomb':
          mod.poisonDpsField += value;
          mod.splashRadius = Math.max(mod.splashRadius, 30);
          mod.weaponType = 'plague_bomb';
          break;
        case 'sun_storm':
          mod.orbCount += value;
          mod.bulletSizeMul *= 1.5;
          mod.weaponType = 'sun_storm';
          break;
        case 'photon_cannon':
          mod.laserDps += value;
          mod.attackSpeedMul *= 0.7;
          mod.weaponType = 'photon_cannon';
          break;
        case 'soul_reaper':
          mod.executeThreshold = Math.max(mod.executeThreshold, value);
          mod.splashRadius = Math.max(mod.splashRadius, 20);
          mod.weaponType = 'soul_reaper';
          break;

        // ── Fusion Defenses ──
        case 'wind_runner':
          mod.speedMul *= value;
          mod.healPerTick += Math.floor(value * 0.5);
          mod.healInterval = 1000;
          break;
        case 'shield_bash':
          mod.shieldOrbs += value;
          mod.reflectChance = Math.max(mod.reflectChance, 0.3);
          break;
        case 'void_walker':
          mod.ghostStepDuration = Math.max(mod.ghostStepDuration, value);
          mod.ghostStepInterval = 4000;
          mod.speedMul *= 1.2;
          break;
        case 'black_hole':
          mod.repulseRadius = Math.max(mod.repulseRadius, value);
          mod.magnetRange += value * 0.5;
          break;
        case 'absolute_defense':
          mod.shieldOrbs += value;
          mod.reflectChance = Math.max(mod.reflectChance, 0.5);
          mod.shieldReduction = Math.max(mod.shieldReduction, 0.3);
          break;
        case 'angel_embrace':
          mod.holyGuardThreshold = 0.5;
          mod.holyGuardHealRatio = Math.max(mod.holyGuardHealRatio, value);
          mod.ghostStepDuration = Math.max(mod.ghostStepDuration, 3.0);
          break;

        // ── Fusion Augments ──
        case 'nuclear_core':
          mod.damageMul *= value;
          mod.splashRadius = Math.max(mod.splashRadius, 50);
          break;
```

Also update the `pickupItem` method to handle fusion items as valid pickups. In the `pickupItem` method, the check `const def = ALL_ITEMS[itemId]` on line 157 will now find fusion items, and the weapon replacement logic on line 162 checks `d?.category === 'weapon'` which will work for fusion weapons too. The weapon replacement check `existingWeapon.id !== itemId` (line 167) will correctly treat fusion weapons as different from base weapons.

However, we need to ensure fusion weapons can upgrade (stack) like other items. Currently fusion outputs always start at level 1. If a player picks up another of the same fusion item (which shouldn't normally happen since fusion items don't drop — they only come from fusion), the existing `existing` check on line 186 handles this.

No additional changes needed to `pickupItem`.

- [ ] **Step 3: Commit**

```bash
git add src/data/items.ts src/systems/ItemSystem.ts
git commit -m "fix: add fusion item definitions and modifier effects

Fusion output items had no ItemDef entries, making all fusion weapons
completely non-functional. Added 20 fusion item definitions to items.ts
and corresponding modifier cases in ItemSystem.recalculate()."
```

---

## Task 2: Fix Double Kill Scoring (CRITICAL)

**Files:**
- Modify: `src/entities/Enemy.ts` — Add `scored` flag
- Modify: `src/scenes/GameScene.ts` — Check `scored` flag before scoring

Multiple damage sources can call `onEnemyKilled` for the same enemy, causing double score, double drops, and double kill counts.

- [ ] **Step 1: Add `scored` flag to Enemy class**

In `src/entities/Enemy.ts`, add the `scored` field after `isAlive` (line 27):

```typescript
  isAlive: boolean;
  scored: boolean;
```

In the constructor (line 48), initialize it:

```typescript
    this.isAlive = true;
    this.scored = false;
```

In the `die()` method (line 238), keep it unchanged — `scored` doesn't need reset in `die()` since the enemy is dead.

- [ ] **Step 2: Guard all kill-scoring call sites in GameScene**

In `src/scenes/GameScene.ts`, wrap all `onEnemyKilled` + `addKill` calls with a `scored` guard.

**a) Shield orb kill (lines 409-412):**

Replace:
```typescript
          if (!enemy.isAlive) {
            this.scoreSystem.addKill(enemy.score);
            this.onEnemyKilled(enemy);
          }
```

With:
```typescript
          if (!enemy.isAlive && !enemy.scored) {
            enemy.scored = true;
            this.scoreSystem.addKill(enemy.score);
            this.onEnemyKilled(enemy);
          }
```

**b) Bullet hit kill (lines 477-483):**

Replace:
```typescript
    if (!enemy.isAlive) {
      this.scoreSystem.addKill(enemy.score);
      this.onEnemyKilled(enemy);
      // Kill feedback
      this.combatFeedback.onKill(isCrit);
      this.combatFeedback.deathExplosion(enemy.x, enemy.y, 0xffaa44, isCrit ? 8 : 5);
    }
```

With:
```typescript
    if (!enemy.isAlive && !enemy.scored) {
      enemy.scored = true;
      this.scoreSystem.addKill(enemy.score);
      this.onEnemyKilled(enemy);
      // Kill feedback
      this.combatFeedback.onKill(isCrit);
      this.combatFeedback.deathExplosion(enemy.x, enemy.y, 0xffaa44, isCrit ? 8 : 5);
    }
```

**c) Chain lightning kill (lines 498-500):**

Replace:
```typescript
        if (chainsLeft > 1 && !target.isAlive) {
          this.scoreSystem.addKill(target.score);
          this.onEnemyKilled(target);
        }
```

With:
```typescript
        if (chainsLeft > 1 && !target.isAlive && !target.scored) {
          target.scored = true;
          this.scoreSystem.addKill(target.score);
          this.onEnemyKilled(target);
        }
```

**d) onOrbKill and onWeaponKill callbacks (lines 79-88):**

Replace:
```typescript
    this.shootSystem.onOrbKill = (enemy: Enemy) => {
      this.scoreSystem.addKill(enemy.score);
      this.onEnemyKilled(enemy);
    };
    this.shootSystem.onWeaponKill = (enemy: Enemy) => {
      this.scoreSystem.addKill(enemy.score);
      this.onEnemyKilled(enemy);
      this.combatFeedback.onKill(false);
      this.combatFeedback.deathExplosion(enemy.x, enemy.y, 0xffaa44, 4);
    };
```

With:
```typescript
    this.shootSystem.onOrbKill = (enemy: Enemy) => {
      if (enemy.scored) return;
      enemy.scored = true;
      this.scoreSystem.addKill(enemy.score);
      this.onEnemyKilled(enemy);
    };
    this.shootSystem.onWeaponKill = (enemy: Enemy) => {
      if (enemy.scored) return;
      enemy.scored = true;
      this.scoreSystem.addKill(enemy.score);
      this.onEnemyKilled(enemy);
      this.combatFeedback.onKill(false);
      this.combatFeedback.deathExplosion(enemy.x, enemy.y, 0xffaa44, 4);
    };
```

- [ ] **Step 3: Commit**

```bash
git add src/entities/Enemy.ts src/scenes/GameScene.ts
git commit -m "fix: prevent double kill scoring from overlapping damage sources

Added 'scored' flag to Enemy. All kill-scoring call sites now check
the flag before adding score/drops, preventing double rewards when
multiple damage sources kill the same enemy in the same frame."
```

---

## Task 3: Fix Photon Cannon Memory Leak (HIGH)

**Files:**
- Modify: `src/systems/AutoShootSystem.ts` — Reuse a single impact circle instead of creating one per frame

The `updateLaserBeam` method creates a new `Phaser.GameObjects.Circle` every frame when `isPhoton` is true (~60/sec). Each has a tween that destroys it, but this causes severe GC pressure.

- [ ] **Step 1: Add a reusable photon impact circle field**

In `src/systems/AutoShootSystem.ts`, add a new field near the `laserGraphics` field (around line 38):

```typescript
  // Laser beam
  laserGraphics: Phaser.GameObjects.Graphics | null = null;
  laserTarget: Enemy | null = null;
  photonImpactCircle: Phaser.GameObjects.Arc | null = null;
```

- [ ] **Step 2: Replace per-frame circle creation with reusable circle**

Replace the photon cannon impact section (lines 932-943):

```typescript
    // Photon cannon: pulsing glow on target
    if (isPhoton && nearest.active) {
      const pulseAlpha = 0.3 + Math.sin(time / 50) * 0.2;
      const impactCircle = this.scene.add.circle(
        nearest.x, nearest.y, 8, 0xffaaff, pulseAlpha,
      ).setDepth(9);
      this.scene.tweens.add({
        targets: impactCircle,
        alpha: 0, scaleX: 2, scaleY: 2,
        duration: 150, onComplete: () => impactCircle.destroy(),
      });
    }
```

With:

```typescript
    // Photon cannon: pulsing glow on target (reuse single circle)
    if (isPhoton && nearest.active) {
      const pulseAlpha = 0.3 + Math.sin(time / 50) * 0.2;
      if (!this.photonImpactCircle) {
        this.photonImpactCircle = this.scene.add.circle(
          nearest.x, nearest.y, 8, 0xffaaff, pulseAlpha,
        ).setDepth(9);
      } else {
        this.photonImpactCircle.setPosition(nearest.x, nearest.y);
        this.photonImpactCircle.setAlpha(pulseAlpha);
        this.photonImpactCircle.setScale(1);
      }
      this.laserTarget = nearest;
    } else {
      if (this.photonImpactCircle) {
        this.photonImpactCircle.destroy();
        this.photonImpactCircle = null;
      }
      this.laserTarget = null;
    }
```

- [ ] **Step 3: Clean up in clearLaser and destroy**

In `clearLaser()` (line 946), add cleanup:

```typescript
  private clearLaser() {
    if (this.laserGraphics) {
      this.laserGraphics.clear();
    }
    if (this.photonImpactCircle) {
      this.photonImpactCircle.destroy();
      this.photonImpactCircle = null;
    }
  }
```

In `destroy()` (around line 1045), add cleanup after `this.clearLaser()`:

The `destroy()` already calls `this.clearLaser()` so the photonImpactCircle cleanup in `clearLaser()` handles it. No additional change needed in `destroy()`.

- [ ] **Step 4: Commit**

```bash
git add src/systems/AutoShootSystem.ts
git commit -m "fix: photon cannon memory leak from per-frame circle creation

Replaced per-frame Circle+Tween creation with a single reusable circle
that repositions each frame. Eliminates ~60 object allocations/second."
```

---

## Task 4: Fix Fusion Items Invisible in HUD (HIGH)

**Depends on:** Task 1 (fusion items must be in ALL_ITEMS)

**Files:**
- Modify: `src/ui/HUD.ts` — Display fusion items with recipe info

Fusion items are now in `ALL_ITEMS` (after Task 1), so `ALL_ITEMS[skill.id]` will return a valid def. However, fusion items don't have entries in `ITEM_ICON_MAP`, so their icons won't show. We need a fallback display.

- [ ] **Step 1: Add fusion icon mapping fallback in HUD**

In `src/ui/HUD.ts`, add an import at the top:

```typescript
import { ALL_ITEMS, ItemCategory } from '../data/items';
import { FUSION_RECIPES } from '../data/fusionRecipes';
```

Then add a helper function after the `ITEM_ICON_MAP` constant (after line 48):

```typescript
/** Get icon key for an item, falling back to base category icon for fusion items */
function getItemIconKey(itemId: string): string | null {
  if (ITEM_ICON_MAP[itemId]) return ITEM_ICON_MAP[itemId];
  // Fusion items: use first input's icon as fallback
  const recipe = FUSION_RECIPES.find(r => r.output === itemId);
  if (recipe) {
    const firstInputIcon = ITEM_ICON_MAP[recipe.inputs[0]];
    if (firstInputIcon) return firstInputIcon;
  }
  return null;
}
```

- [ ] **Step 2: Use the helper in the update loop**

In the `update` method (line 180), replace:

```typescript
          const iconKey = ITEM_ICON_MAP[skill.id];
```

With:

```typescript
          const iconKey = getItemIconKey(skill.id);
```

Also, the update loop currently hardcodes `i < 5`. We need to handle the case where the player has more than 5 skills. For now, we show up to the number of skill icon slots available. The dynamic slot count fix is in Task 5.

- [ ] **Step 3: Commit**

```bash
git add src/ui/HUD.ts
git commit -m "fix: display fusion items in HUD with fallback icons

Fusion items now appear in the HUD skill bar using their first input's
icon as a visual fallback when no dedicated icon exists."
```

---

## Task 5: Fix HUD Dynamic Skill Slots (HIGH)

**Depends on:** Task 4 (fusion icon fallback)

**Files:**
- Modify: `src/ui/HUD.ts` — Make skill bar dynamic based on player skill count

The HUD creates exactly 5 skill icon slots in the constructor and iterates 5 in the update loop. With `fusionSlotBonus`, the player can have more than 5 skills.

- [ ] **Step 1: Add a maxSlots field and refactor constructor to support dynamic slots**

In `src/ui/HUD.ts`, add a `maxSlots` field to the HUD class:

```typescript
export class HUD {
  scene: Phaser.Scene;
  sw: number;
  sh: number;
  fontSize: number;
  smallFont: number;
  maxSlots: number;
  // ... existing fields ...
```

In the constructor, change the skill bar creation. Replace lines 118-152 (from `const slotSize = ...` to the end of the `for` loop) with:

```typescript
    // Skill bar (bottom center) — initially 5 slots, grows with fusion bonuses
    this.maxSlots = 5;
    const slotSize = Math.round(40 * scale);
    const slotGap = Math.round(5 * scale);

    const skillBarW = this.maxSlots * (slotSize + slotGap) + slotGap;
    const skillBarH = slotSize + slotGap * 2;
    const skillBarX = this.sw / 2 - skillBarW / 2;
    const skillBarY = this.sh - skillBarH - pad;

    this.skillBarBg = scene.add.rectangle(
      skillBarX, skillBarY, skillBarW, skillBarH, 0x111122, 0.9
    ).setOrigin(0, 0).setDepth(50).setScrollFactor(0)
      .setStrokeStyle(1, 0x444466, 0.6);

    this.skillBarX = skillBarX;
    this.skillBarY = skillBarY;
    this.slotSize = slotSize;
    this.slotGap = slotGap;
    this.pad = pad;

    for (let i = 0; i < this.maxSlots; i++) {
      this.createSkillSlot(skillBarX + slotGap + i * (slotSize + slotGap), skillBarY + slotGap, slotSize, scale);
    }
```

Add the stored layout fields to the class:

```typescript
  skillBarX: number;
  skillBarY: number;
  slotSize: number;
  slotGap: number;
  pad: number;
```

Add a `createSkillSlot` helper method:

```typescript
  private createSkillSlot(x: number, y: number, slotSize: number, scale: number) {
    const bg = this.scene.add.rectangle(x, y, slotSize, slotSize, 0x222244)
      .setStrokeStyle(2, 0x444466).setDepth(51).setScrollFactor(0);

    const catBg = this.scene.add.rectangle(x + 2, y + 2, slotSize - 4, slotSize - 4, 0x333355)
      .setDepth(51.5).setScrollFactor(0).setVisible(false);

    const iconScale = slotSize / 20;
    const image = this.scene.add.image(x + slotSize / 2, y + slotSize / 2, 'item_pickup')
      .setScale(iconScale).setDepth(52).setScrollFactor(0).setVisible(false);

    const level = this.scene.add.text(x + slotSize - 3, y + slotSize - 3, '', {
      fontSize: `${Math.max(9, Math.round(10 * scale))}px`,
      color: '#ffdd44', fontFamily: 'monospace', fontStyle: 'bold',
      backgroundColor: '#000000cc', padding: { x: 3, y: 1 },
    }).setOrigin(1, 1).setDepth(53).setScrollFactor(0);

    this.skillIcons.push({ bg, catBg, image, level });
  }
```

- [ ] **Step 2: Add dynamic expansion in update method**

At the start of the `update` method, add dynamic slot expansion:

```typescript
  update(player: Player, scoreSystem: ScoreSystem, waveSystem: WaveSystem, time: number) {
    // Expand skill bar if player has more skills than current slots
    const neededSlots = Math.max(5, player.skills.length);
    if (neededSlots > this.maxSlots) {
      const scale = Math.min(this.sw / 800, this.sh / 600);
      for (let i = this.maxSlots; i < neededSlots; i++) {
        const x = this.skillBarX + this.slotGap + i * (this.slotSize + this.slotGap);
        const y = this.skillBarY + this.slotGap;
        this.createSkillSlot(x, y, this.slotSize, scale);
      }
      this.maxSlots = neededSlots;

      // Resize background bar
      const newBarW = this.maxSlots * (this.slotSize + this.slotGap) + this.slotGap;
      this.skillBarBg.width = newBarW;
      this.skillBarBg.x = this.sw / 2 - newBarW / 2;
    }

    // ... rest of update ...
```

Also change the skill icon iteration loop from `for (let i = 0; i < 5; i++)` to `for (let i = 0; i < this.maxSlots; i++)`:

```typescript
    for (let i = 0; i < this.maxSlots; i++) {
```

- [ ] **Step 3: Commit**

```bash
git add src/ui/HUD.ts
git commit -m "fix: HUD skill bar dynamically expands for fusion bonus slots

Skill bar now grows to accommodate extra slots from fusion bonuses.
Previously hardcoded to 5, hiding any skills beyond that."
```

---

## Task 6: Fix Unconditional Enemy Spawn Bypassing Wave Budget (HIGH)

**Files:**
- Modify: `src/scenes/GameScene.ts` — Add wave budget check and count tracking for emergency spawns

The unconditional spawn at `enemies.countActive() < 3` bypasses the wave budget counter `enemiesSpawned`.

- [ ] **Step 1: Add wave budget check to emergency spawn**

In `src/scenes/GameScene.ts`, replace lines 292-294:

```typescript
    if (this.enemies.countActive() < 3) {
      this.spawnSystem.spawnEnemy(waveConfig);
    }
```

With:

```typescript
    if (this.enemies.countActive() < 3 && this.waveSystem.canSpawnMore()) {
      this.spawnSystem.spawnEnemy(waveConfig);
    }
```

- [ ] **Step 2: Add `canSpawnMore()` method to WaveSystem**

In `src/systems/WaveSystem.ts`, add a new method after `shouldSpawn()` (after line 63):

```typescript
  /** Check if more enemies can be spawned this wave (no side effect) */
  canSpawnMore(): boolean {
    return this.enemiesSpawned < this.waveConfig.enemyCount;
  }
```

- [ ] **Step 3: Commit**

```bash
git add src/scenes/GameScene.ts src/systems/WaveSystem.ts
git commit -m "fix: emergency enemy spawn respects wave budget

Added canSpawnMore() query method to WaveSystem. Emergency spawns
(when < 3 enemies alive) now check wave budget instead of spawning
unconditionally."
```

---

## Task 7: Fix Pickup Magnet Physics Bypass (MEDIUM)

**Files:**
- Modify: `src/scenes/GameScene.ts` — Use `setVelocity` instead of direct position mutation

- [ ] **Step 1: Replace direct position mutation with velocity-based movement**

In `src/scenes/GameScene.ts`, replace lines 336-339:

```typescript
      if (dist < pickupRange) {
        const angle = Math.atan2(this.player.y - pickup.y, this.player.x - pickup.x);
        pickup.x += Math.cos(angle) * 3;
        pickup.y += Math.sin(angle) * 3;
      }
```

With:

```typescript
      if (dist < pickupRange) {
        const angle = Math.atan2(this.player.y - pickup.y, this.player.x - pickup.x);
        const speed = 200 * (1 - dist / pickupRange);
        const body = pickup.body as Phaser.Physics.Arcade.Body;
        if (body) {
          body.setVelocity(Math.cos(angle) * speed, Math.sin(angle) * speed);
        }
      } else {
        const body = pickup.body as Phaser.Physics.Arcade.Body;
        if (body) {
          body.setVelocity(0, 0);
        }
      }
```

- [ ] **Step 2: Commit**

```bash
git add src/scenes/GameScene.ts
git commit -m "fix: pickup magnet uses physics velocity instead of position mutation

Direct position mutation bypassed the physics engine, potentially
causing overlap detection to miss pickups. Now uses setVelocity()."
```

---

## Task 8: Fix Slow Effect Overwrite (MEDIUM)

**Files:**
- Modify: `src/entities/Enemy.ts` — Keep stronger slow instead of always overwriting

- [ ] **Step 1: Keep the stronger slow effect**

In `src/entities/Enemy.ts`, replace `applyEffect` (lines 101-109):

```typescript
  applyEffect(effect: StatusEffect) {
    const existing = this.effects.find(e => e.type === effect.type);
    if (existing) {
      existing.value = effect.value;
      existing.duration = effect.duration;
      existing.startTime = effect.startTime;
    } else {
      this.effects.push({ ...effect });
    }
  }
```

With:

```typescript
  applyEffect(effect: StatusEffect) {
    const existing = this.effects.find(e => e.type === effect.type);
    if (existing) {
      // Keep the stronger effect: higher slow value or longer poison
      const isStronger = effect.type === 'slow'
        ? effect.value > existing.value
        : effect.value > existing.value;
      if (isStronger || effect.duration > existing.duration - (this.scene.time.now - existing.startTime)) {
        existing.value = effect.value;
        existing.duration = effect.duration;
        existing.startTime = effect.startTime;
      }
    } else {
      this.effects.push({ ...effect });
    }
  }
```

- [ ] **Step 2: Commit**

```bash
git add src/entities/Enemy.ts
git commit -m "fix: slow effects keep stronger value instead of overwriting

Previously, a weaker slow applied later would replace a stronger one.
Now the stronger effect is preserved."
```

---

## Task 9: Fix Fallback Spawn Position in Wall (MEDIUM)

**Files:**
- Modify: `src/systems/SpawnSystem.ts` — Validate fallback position against dungeon grid

- [ ] **Step 1: Validate ultimate fallback position**

In `src/systems/SpawnSystem.ts`, replace the ultimate fallback (lines 181-182):

```typescript
    // Ultimate fallback: player position + offset
    return { x: this.playerX + 100, y: this.playerY + 100 };
```

With:

```typescript
    // Ultimate fallback: player position + offset, validated against grid
    for (let offset = 64; offset < 300; offset += 32) {
      for (let angle = 0; angle < Math.PI * 2; angle += Math.PI / 4) {
        const fx = this.playerX + Math.cos(angle) * offset;
        const fy = this.playerY + Math.sin(angle) * offset;
        if (this.isFloor(fx, fy)) {
          return { x: fx, y: fy };
        }
      }
    }
    // Absolute fallback: player position itself
    return { x: this.playerX, y: this.playerY };
```

- [ ] **Step 2: Commit**

```bash
git add src/systems/SpawnSystem.ts
git commit -m "fix: fallback spawn validates floor position instead of blind offset

Previously added +100/+100 to player position which could land inside
a wall. Now tries multiple angles/distances, checking isFloor()."
```

---

## Task 10: Fix Fragment Rarity Overlap at Wave 18 (MEDIUM)

**Files:**
- Modify: `src/data/fragments.ts` — Change legendary range to start at 19 to avoid overlap

- [ ] **Step 1: Fix wave 18 overlap**

In `src/data/fragments.ts`, change the legendary fragment's `dropWaveRange` from `[18, 999]` to `[19, 999]`:

```typescript
  legendary: {
    rarity: 'legendary',
    name: '金色碎片',
    color: '#ffaa00',
    dropWaveRange: [19, 999],
    dropRate: 0.02,
    requiredToUnlock: 20,
    unlockableRarities: ['legendary'],
  },
```

- [ ] **Step 2: Commit**

```bash
git add src/data/fragments.ts
git commit -m "fix: legendary fragments now start at wave 19, not 18

Epic fragments have range [13,18] and legendary had [18,999], causing
the iteration-order-dependent getFragmentRarityForWave to always
return epic at wave 18."
```

---

## Task 11: Fix Dead Code in Wave Tier Logic (MEDIUM)

**Files:**
- Modify: `src/data/waves.ts` — Remove unreachable branch

- [ ] **Step 1: Remove dead code**

In `src/data/waves.ts`, remove the unreachable `waveNumber < 5` branch. Replace:

```typescript
  let tiers: string[];
  if (waveNumber <= 2) {
    tiers = ['easy'];
  } else if (waveNumber <= 4) {
    tiers = ['easy', 'medium'];
  } else if (waveNumber < 5) {
    tiers = ['medium'];
  } else if (waveNumber % 5 === 0) {
```

With:

```typescript
  let tiers: string[];
  if (waveNumber <= 2) {
    tiers = ['easy'];
  } else if (waveNumber <= 4) {
    tiers = ['easy', 'medium'];
  } else if (waveNumber % 5 === 0) {
```

- [ ] **Step 2: Commit**

```bash
git add src/data/waves.ts
git commit -m "fix: remove unreachable wave tier branch (waveNumber < 5)

This branch was dead code because waveNumber <= 4 was already handled
by the preceding else-if."
```

---

## Task 12: Fix Loading Screen game-ready Event (LOW)

**Files:**
- Modify: `src/scenes/BootScene.ts` — Dispatch `game-ready` event

- [ ] **Step 1: Dispatch game-ready event after textures load**

In `src/scenes/BootScene.ts`, replace:

```typescript
  create() {
    generateAllTextures(this);
    this.scene.start('MenuScene');
  }
```

With:

```typescript
  create() {
    generateAllTextures(this);
    window.dispatchEvent(new Event('game-ready'));
    this.scene.start('MenuScene');
  }
```

- [ ] **Step 2: Commit**

```bash
git add src/scenes/BootScene.ts
git commit -m "fix: dispatch game-ready event after texture generation

The loading screen listens for 'game-ready' to hide itself, but the
event was never fired. It now fires immediately after all textures
are generated, removing the 5-second timeout dependency."
```

---

## Task 13: Fix randomPick and getItemValue Edge Cases (LOW)

**Files:**
- Modify: `src/utils/helpers.ts` — Handle empty arrays
- Modify: `src/data/items.ts` — Guard against level <= 0

- [ ] **Step 1: Fix randomPick to handle empty arrays**

In `src/utils/helpers.ts`, replace:

```typescript
export function randomPick<T>(arr: T[]): T {
  return arr[Math.floor(Math.random() * arr.length)];
}
```

With:

```typescript
export function randomPick<T>(arr: T[]): T {
  if (arr.length === 0) throw new Error('randomPick called with empty array');
  return arr[Math.floor(Math.random() * arr.length)];
}
```

- [ ] **Step 2: Fix getItemValue to guard against level <= 0**

In `src/data/items.ts`, replace:

```typescript
export function getItemValue(id: string, level: number): number {
  const def = ALL_ITEMS[id];
  if (!def) return 0;
  return def.levels[Math.min(level - 1, def.levels.length - 1)];
}
```

With:

```typescript
export function getItemValue(id: string, level: number): number {
  const def = ALL_ITEMS[id];
  if (!def) return 0;
  if (level <= 0) return 0;
  return def.levels[Math.min(level - 1, def.levels.length - 1)];
}
```

- [ ] **Step 3: Commit**

```bash
git add src/utils/helpers.ts src/data/items.ts
git commit -m "fix: guard randomPick empty array and getItemValue level <= 0

randomPick now throws on empty array instead of returning undefined.
getItemValue returns 0 for non-positive levels instead of negative indexing."
```

---

## Task 14: Fix visualViewport any Cast (LOW)

**Files:**
- Modify: `src/main.ts` — Remove unnecessary `any` cast

- [ ] **Step 1: Use proper typing**

In `src/main.ts`, replace:

```typescript
if ('visualViewport' in window) {
  (window as any).visualViewport.addEventListener('resize', () => {
    const vv = (window as any).visualViewport;
    game.scale.resize(vv.width, vv.height);
  });
}
```

With:

```typescript
if (window.visualViewport) {
  window.visualViewport.addEventListener('resize', () => {
    const vv = window.visualViewport!;
    game.scale.resize(vv.width, vv.height);
  });
}
```

- [ ] **Step 2: Commit**

```bash
git add src/main.ts
git commit -m "fix: remove unnecessary any cast on visualViewport"
```

---

## Task Dependency Graph

```
Task 1 (Fusion Item Defs)
  ├── Task 4 (HUD Fusion Display) — depends on Task 1
  └── Task 5 (HUD Dynamic Slots) — depends on Task 4

Task 2 (Double Kill Scoring) — independent
Task 3 (Photon Cannon Leak) — independent
Task 6 (Emergency Spawn Budget) — independent
Task 7 (Pickup Magnet) — independent
Task 8 (Slow Overwrite) — independent
Task 9 (Fallback Spawn) — independent
Task 10 (Fragment Overlap) — independent
Task 11 (Dead Code) — independent
Task 12 (Loading Event) — independent
Task 13 (Edge Cases) — independent
Task 14 (Type Cast) — independent
```
